# Radius Intelligence Layer

**Owned by:** Mikael Hill / Radius (formalize via entity when ready)

## One line

The Router picks the best brain for each moment. The **Spiral Spinner** is what users see — magical thinking, never model names.

## What’s yours (proprietary)

| Layer | Status | Owns |
|-------|--------|------|
| **Router** | v1 stub in `src/lib/router.ts` | Intent → quality / speed / balanced |
| **Orchestrator** | v1 live | Planner → builder → critic loop |
| **Context engine** | v1 live | Priority-packed prompts (Priompt-style) |
| **Visual Stage** | v1 live | Agent feed + live preview + Keep/Undo/Tweak |
| **Spiral Spinner** | v1 live | Brand motion during routing & generation |
| **Feedback flywheel** | Next | Keep / Undo / Tweak → RL signals |
| **Specialist models** | Later | Fine-tune on “kept” outcomes |

## What’s licensed (not owned)

- Frontier LLMs (OpenAI, Anthropic, Google, etc.) — API access only
- Hosting (Vercel), storage — infrastructure

## Router rules (v1)

- **Tweak / revision** → speed mode — fast refine
- **Rich brief** (long prompt or assets) → quality mode — best builder
- **Default** → balanced

Users never see model IDs. They see the **Radius Spiral** and plain English in the agent feed.

## 90-day build order (aggressive)

1. Router v1 + Spiral everywhere agents work ✅ (started)
2. Log Keep/Undo/Tweak per session
3. User memory (brand, past sites)
4. Router v2 — learned routing from feedback
5. Export + custom domains
6. First specialist fine-tune on kept sites

## IP note

Copyright: code, UI, copy, Spiral motion design, docs.  
Trademark: Radius, logotype, Spiral mark.  
Patents: consult counsel if routing + speculative-apply methods are novel enough to file.

**Not claiming AGI.** Building a **compound intelligence** that feels superhuman for everyday users.
